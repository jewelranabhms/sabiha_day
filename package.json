{
  "name": "sabiha-system",
  "private": true,
  "version": "0.1.0",
  "description": "Sabiha's Journey (public website) + Sabiha's Day (private app) + Admin dashboard",
  "workspaces": [
    "website"
  ],
  "scripts": {
    "dev": "npm run dev --workspace website",
    "build": "npm run build --workspace website",
    "start": "npm run start --workspace website",
    "mobile": "cd app && npm install && npx expo start",
    "seed": "npm run seed --workspace website"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/hind-siliguri": "^5.3.0",
    "next": "^15.5.25"
  }
}
